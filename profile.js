// PatriaSoul — renderer profila. Čita samo centralni PatriaPlayer/PatriaLevels/PatriaBadges.
(function(){'use strict';document.addEventListener('DOMContentLoaded',()=>{
 const root=document.getElementById('profile');if(!root||!window.PatriaPlayer)return;const p=PatriaPlayer.current(),levels=window.PatriaLevels,progress=levels?.progress(p.xp)||{current:{name:'Početnik',minXP:0},next:null,percent:100,xpToNext:0};
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const badges=window.PatriaBadges?.all?.()||[];const earned=new Set(p.badges||[]);const cats=Object.entries(p.categories||{}).sort((a,b)=>(b[1].points||0)-(a[1].points||0));
 root.innerHTML=`<article class="ps-card"><div class="ps-card-icon">🏆</div><p class="ps-kicker">Razina ${progress.current.id}</p><h2>${esc(progress.current.name)}</h2><p><strong>${p.xp} XP</strong>${progress.next?` · još ${progress.xpToNext} XP do ${esc(progress.next.name)}`:' · maksimalna razina'}</p><div class="ps-progress"><span style="display:block;width:${progress.percent}%;height:8px"></span></div><small>${progress.percent}% napretka</small></article><article class="ps-card"><div class="ps-card-icon">📊</div><h2>Statistika</h2><p>Kvizovi <strong>${p.quizzes}</strong></p><p>Točni odgovori <strong>${p.correct}/${p.answers}</strong></p><p>Bodovi <strong>${p.points}</strong></p><p>Streak <strong>🔥 ${p.streak}</strong></p></article><article class="ps-card"><div class="ps-card-icon">🎖️</div><h2>Značke</h2><p>${earned.size}/${badges.length} osvojenih</p><div>${badges.map(b=>`<span title="${esc(b.description)}" style="display:inline-block;opacity:${earned.has(b.id)?1:.22};font-size:28px;margin:4px">${b.icon}</span>`).join('')}</div></article><article class="ps-card" style="grid-column:1/-1"><div class="ps-card-icon">📚</div><h2>Kategorije</h2>${cats.length?cats.map(([name,c])=>`<div style="display:grid;grid-template-columns:minmax(120px,1fr) auto;gap:10px;padding:9px 0;border-bottom:1px solid rgba(255,255,255,.08)"><span>${esc(name)}</span><strong>${c.points||0} bod · ${c.correct||0}/${c.answers||0}</strong></div>`).join(''):'<p>Odigraj prvi kviz da se statistika počne puniti.</p>'}</article><article class="ps-card" style="grid-column:1/-1"><h2>Omiljena pitanja</h2><p>${p.favorites.length?`Spremljeno ${p.favorites.length} pitanja.`:'Još nema omiljenih pitanja.'}</p></article>`;
});})();
