name: Audit PatriaSoul links and runtime dependencies

on:
  push:
    branches: [main]
    paths:
      - '**.html'
      - '**.js'
      - '**.css'
      - 'scripts/check-internal-links.py'
      - 'scripts/check-runtime-dependencies.py'
      - 'sw.js'
      - 'site-navigation.js'
      - 'navigation-ux.js'
  pull_request:
    paths:
      - '**.html'
      - '**.js'
      - '**.css'
      - 'scripts/check-internal-links.py'
      - 'scripts/check-runtime-dependencies.py'
      - 'sw.js'
      - 'site-navigation.js'
      - 'navigation-ux.js'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run internal-link audit
        run: python3 scripts/check-internal-links.py
      - name: Run runtime dependency and PWA shell audit
        run: python3 scripts/check-runtime-dependencies.py
