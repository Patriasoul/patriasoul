// PatriaSoul — Brani svoj grad engine. Koristi centralni gradovi + quiz + profil sustav.
(function(global){'use strict';const KEY='patriasoul_city_results_v2';function read(){try{const x=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(x)?x:[]}catch(_){return []}}function save(x){localStorage.setItem(KEY,JSON.stringify(x.slice(-1000)));return x}function start(city){const seed=[...String(city)].reduce((a,c)=>(a*31+c.charCodeAt(0))>>>0,17);return global.PatriaQuiz.seededShuffle(global.PatriaQuiz.bank(),seed).slice(0,5).map(q=>global.PatriaQuiz.prepare(q))}function finish(city,score,correct){const p=global.PatriaPlayer.current();global.PatriaPlayer.addCityScore(city,score);const result={id:crypto.randomUUID?.()||Date.now()+'',city,score,correct,answers:5,name:p.name||'Gost',date:new Date().toISOString(),period:'daily'};save(read().concat(result));global.PatriaPlayer.recordResult({category:'brani-svoj-grad',points:score,xp:score,correct,answers:5,city,period:'daily',id:result.id});global.PatriaBadges?.evaluate(global.PatriaPlayer.current());return result}function cityRows(city){return read().filter(r=>!city||r.city===city).sort((a,b)=>b.score-a.score)}global.PatriaCityGame={start,finish,results:read,cityRows}})(typeof window!=='undefined'?window:globalThis);
